<!DOCTYPE html>
<html lang="en">
  <%- include('layouts/head')%>
  <body>
    <%- include('layouts/header')%>
    <main>
      <h1 class="color-green">
        Welcome to <% if (editing) { %> edit <% } else { %> add <% } %> blog
        page.
      </h1>
      <form
        class="blog-form"
        action="/<% if (editing) { %>edit-blog<% } else { %>add-blog<% } %>"
        method="POST"
      >
        <div class="form-control">
          <label for="title">Title</label>
          <input
            type="text"
            name="title"
            id="title"
            value="<% if (editing) { %><%= blog.title %><% } %>"
          />
        </div>
        <div class="form-control">
          <label for="content">Content</label>
          <input
            type="text"
            name="content"
            id="content"
            value="<% if (editing) { %><%= blog.content %><% } %>"
          />
        </div>
        <div class="form-control">
          <label for="author">Author</label>
          <input
            type="text"
            name="author"
            id="author"
            value="<% if (editing) { %><%= blog.author %><% } %>"
          />
        </div>
        <div class="form-control">
          <label for="creationDate">Creation Date</label>
          <input
            type="text"
            name="creationDate"
            id="creationDate"
            value="<% if (editing) { %><%= blog.creationDate %><% } %>"
          />
        </div>
        <% if (editing) { %>
        <input type="hidden" value="<%= blog._id %>" name="blogId" />
        <% } %>
        <button class="btn" type="submit">
          <% if (editing) { %>Update blog<% } else { %>Add blog<% } %>
        </button>
      </form>
    </main>
    <%- include('layouts/footer')%>
  </body>
</html>
